@using Siuuuu.Vm
@model HomePageVm
@{
    ViewData["Title"] = "Home Page";
}
        <header id="hea">
              <div class="centeredd">
            <img src="~/Pages/image/sln.png" alt="">
        </div>
                </header>


 <section id="homeOz">
        <div class="container">
            <div class="row">
                @foreach (var item in Model.homeTxt1)
               {
                   
               
                <div class="col-lg-12">
                    <div class="MarginTopp">
                        <h2 style="text-align: center ;">@item.Title</h2>
                    </div>
                </div>
                <div class="col-lg-2"></div>
                <div class="col-lg-4">
                    <p> @item.Description</p>
                </div>   
                <div class="col-lg-4">
                    <p>                    @item.Description2
</p>
                            </div>

                <div class="col-lg-2"></div>
                }
            </div>
        </div>
    </section>
    <section id="introo">
        @foreach (var item in Model.homeImageOne)
       {
           
       
        <div class="cont">
            <img src="~/Pages/image/@item.ImgUrl" alt="Snow" style="width:87%;">
            <div class="centered">
                <h1 class="musiiik" style="font-size: 40px;">@item.Txt

                </h1>
            </div>
            </div>
       }

    </section>
    <section id="homeOz">
        <div class="container">
            <div class="row">
                @foreach (var item in Model.homeTxt2)
               {
                   
               
                <div class="col-lg-12">
                    <div class="MarginTopp">
                        <h2 style="text-align: center ;">@item.Title
                        </h2>
                    </div>
                </div>
                <div class="col-lg-2"></div>
                <div class="col-lg-4">
                   <p>
                       @item.Description
                    
</p>
                </div>   
                <div class="col-lg-4">
                    <p>@item.Description2

                        </p>
                       </div>

                <div class="col-lg-2"></div>
                }
            </div>
        </div>
    </section>
    <section id="toplar">
        <div class="container">
            <div class="row">
                @foreach (var item in Model.homeIcon)
               {
                   
               
                <div class="col-lg-3 col-md-3 ">
                    <img src="~/Pages/image/@item.ImgUrl" alt="">
                </div>
               }
              
            </div>
        </div>
    </section>
    <section id="introo">
        @foreach (var item in Model.homeImageTwo)
       {
           
       
        <div class="cont">
            <img src="~/Pages/image/@item.ImgUrl" alt="Snow" style="width:87%;">
            <div class="centered">
                <h1 class="musiiik" style="font-size: 40px;">@item.Txt

                </h1>
            </div>
            </div>
       }

    </section>
    <section id="homeOz">
        <div class="container">
            <div class="row">
                @foreach (var item in Model.descriptionOne)
               {
                   
               
                <div class="col-lg-12">
                    <div class="MarginTop">
                        <h1>a</h1>
                    </div>
                </div>
                <div class="col-lg-2"></div>
                <div class="col-lg-4">
                    <h1>Oturacaq</h1><br><br>
                    <p>Sliders-də biz gündəlik düzəlişləri nəzərə alırıq və spontan istəkləri təmin edirik, buna görə də məhdud masa rezervasiyalarımız var. Buna görə də, istədiyiniz vaxt dayana bilərsiniz və biz dəbdə ən yaxşısını tapmağınız üçün əlimizdən gələni edəcəyik.Söhbət əminlik və sıx cədvəllərə </p>
                </div>   
                <div class="col-lg-4">
                    <h1>Rezervasyon</h1><br><br>
                    <p> gəldikdə, biz tam başa düşürük. Buna görə də, biz böyük və kiçik şirkətlər üçün bir masa təmin etmək üçün məhdud imkan təklif edirik. Nə qədər ki, yaxşı vaxtda olsanız, şanslar yaxşıdır. Ziyarət etmək istədiyiniz restorana aşağıdakı linklərdən daxil olun..</p>
                    <div class="homeLink" style="display: flex;justify-content: space-between; width: 200px;">
                        <div class="homeLeft"><h5><a href="@item.Title">Link</a></h5></div>
                        <div class="homeRight"><h5><a asp-action="Index" asp-controller="Booking" >Rezervasyon</a></h5></div>
    
    
                    </div>
                </div>
              

                <div class="col-lg-2"></div>
                }
            </div>
        </div>
    </section>
 
    <section id="menuuuuuu " style="z-index: -200;">
        <h1 class="title">Best</h1>
        <div class="slider" x-data="{start: true, end: false}">
            <div class="slider__content" x-ref="slider"
                x-on:scroll.debounce="$refs.slider.scrollLeft == 0 ? start = true : start = false; Math.abs(($refs.slider.scrollWidth - $refs.slider.offsetWidth) - $refs.slider.scrollLeft) < 5 ? end = true : end = false;">
               @foreach (var item in Model.food)
              {
                  
              
                <div class="slider__item">
                    <img class="slider__image" src="~/Pages/image/@item.ImgUrl" alt="Image">
                    <div class="slider__info">
                        <h2>@item.Txt </h2>
                    </div>
                </div>
              }
            </div>
        </div>
        <div class="slider__nav">
            <button class="slider__nav__button"
                x-on:click="$refs.slider.scrollBy({left: $refs.slider.offsetWidth * -1, behavior: 'smooth'});"
                x-bind:class="start ? '' : 'slider__nav__button--active'">Previous</button>
            <button class="slider__nav__button"
                x-on:click="$refs.slider.scrollBy({left: $refs.slider.offsetWidth, behavior: 'smooth'});"
                x-bind:class="end ? '' : 'slider__nav__button--active'">Next</button>
        </div>
        </div>
    </section>